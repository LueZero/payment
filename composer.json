{
    "name": "zero/payment",
    "description": "A lightweight PHP payment integration package that supports multiple payment providers.",
    "require": {
        "php": ">=7.0.0",
        "ext-curl": "*"
    },
    "autoload": {
        "psr-4": {
            "Zero\\": "src"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10"
    }
}
